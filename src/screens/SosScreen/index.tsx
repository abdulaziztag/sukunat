import {View} from 'react-native';
import {Text} from 'react-native-paper';

export const SosScreen = () => {
  return (
    <View>
      <Text variant={'displaySmall'}>SOS Screen</Text>
    </View>
  );
};
