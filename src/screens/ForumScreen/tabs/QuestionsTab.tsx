import {View} from 'react-native';
import {Text} from 'react-native-paper';

export const QuestionsTab = () => {
  return (
    <View style={{flex: 1}}>
      <Text variant={'displaySmall'}>Questions</Text>
    </View>
  );
};
